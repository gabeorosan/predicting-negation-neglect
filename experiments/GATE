Stop condition fired 2026-09-26 (local 0.5B testbed; RUN_LOG entry "Stop fired (local testbed)"): plain's two document
orders differ by more than direct negation's gap to plain in either order, so single-order local comparisons of the
Holloway-specific part are not readable. Nothing is launched until Gabriel replies; remove this file after his reply.
